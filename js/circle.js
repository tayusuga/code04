function R (length){
     var result;
     result=Math.PI * length * 2;
     
   
     return result;
}

function M (length){
    var result;
    result = length * length * Math.PI;
    return result;
}